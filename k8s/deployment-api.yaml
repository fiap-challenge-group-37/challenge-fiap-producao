apiVersion: apps/v1
kind: Deployment
metadata:
  name: lanchonete-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lanchonete_app
  template:
    metadata:
      labels:
        app: lanchonete_app
    spec:
      containers:
        - name: api
          image: 471112881043.dkr.ecr.us-east-1.amazonaws.com/lanchonete-app:latest

          imagePullPolicy: Always

          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 250m
              memory: 512Mi